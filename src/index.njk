{% extends 'src/partials/layout.njk' %}
{% block title %}Home page{% endblock %}
{% block content %}
  <h1>Hello {{ name }}</h1>
  <div class="columns is-multiline">
    {% for character in characters %}
      <div class="column is-one-quarter">
        <div class="card">
          <header class="card-header">
            <p class="card-header-title">{{ character.name }}</p>
          </header>
          <div class="card-image">
            <figure class="image is-4by3">
              <img src="{{ character.image }}" alt="Placeholder image"/>
            </figure>
          </div>
          <div class="card-content">
            <div class="content"></div>
              <table>
                <body>
                  <tr>
                    <th>Status</th>
                    <td>{{ charater.status }}</td>
                  </tr>
                  <tr>
                    <th>Gender</th>
                    <td>{{ charater.gender }}</td>
                  </tr>
                  <tr>
                    <th>Species</th>
                    <td>{{ charater.species }}</td>
                  </tr>
                </body>
              </table>
              <a href="/character_{{ character.id }}.html" class="button is-primary">Read more</a>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
{% endblock %}